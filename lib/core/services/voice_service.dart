// voice_service.dart – replaced by webrtc_voice_service.dart
// Kept as a stub so existing imports don't break during migration.
// Nothing should call this directly; use HybridAudioService instead.

@Deprecated('Use WebRTCVoiceService via HybridAudioService')
class VoiceService {
  VoiceService._();
}
